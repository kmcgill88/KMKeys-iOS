// https://github.com/Quick/Quick

import Quick
import Nimble

import KMKeys

class KMKeysTest: QuickSpec {
    
    override func spec() {
        describe("init KMKeys: default") {
            it("has a toolbar and text field") {
                //let keys = KMKeys()
                //expect(keys.subviews.count) == 2
            }
        }
    }
}
